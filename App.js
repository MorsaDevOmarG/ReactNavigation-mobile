import {
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';
import {
  SafeAreaProvider,
  useSafeAreaInsets,
} from 'react-native-safe-area-context';

export const App = () => {
  return (
    <>
      <Text>App</Text>
    </>
  );
};

const styles = StyleSheet.create({});

export default App;
